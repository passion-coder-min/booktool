{
  "name": "booktool",
  "private": true,
  "version": "0.1.0",
  "description": "本地优先知识出版 + 工作管理工具（Electron + Typst + Mermaid）",
  "scripts": {
    "postinstall": "node scripts/sync-assets.mjs",
    "dev": "pnpm --filter @booktool/desktop dev",
    "build": "pnpm --filter @booktool/desktop build && pnpm --filter @booktool/cli build",
    "typecheck": "tsc --noEmit -p packages/shared/tsconfig.json && tsc --noEmit -p packages/mdtypst/tsconfig.json && tsc --noEmit -p apps/desktop/tsconfig.json && tsc --noEmit -p packages/cli/tsconfig.json",
    "test": "vitest run",
    "booktool": "node packages/cli/dist/cli.js",
    "build:cli": "pnpm --filter @booktool/cli build"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["electron", "esbuild"]
  }
}
